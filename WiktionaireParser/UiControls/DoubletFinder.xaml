<UserControl x:Class="WiktionaireParser.UiControls.DoubletFinder"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WiktionaireParser.UiControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="200"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="11*"/>
                <ColumnDefinition Width="11*"/>
                <ColumnDefinition Width="18*"/>
            </Grid.ColumnDefinitions>
            <ListBox HorizontalAlignment="Left" Height="180" Margin="40,0,0,0" VerticalAlignment="Center" Width="110" d:ItemsSource="{d:SampleData ItemCount=5}"/>
            <ListBox Grid.Column="1" HorizontalAlignment="Left" Height="180" Margin="30,0,0,0" VerticalAlignment="Center" Width="135" d:ItemsSource="{d:SampleData ItemCount=5}"/>
        </Grid>

    </Grid>
</UserControl>
